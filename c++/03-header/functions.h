#pragma once // # -> avaliar pelo pré-processador antes de compilar
             // pragma -> instrução enviada para o pré-processador
             // once -> incluir apenas uma vez o ficheiro functions.h em cada ficheiro .cpp (evitar erros duplicados)

void headerLog(const char *message);